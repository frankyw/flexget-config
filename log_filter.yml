log_filter:
  # 'flexget.components.series.series':
  #   - already configured in series plugin
  #   - configured multiple times in series plugin
  # utils:
  #   - Input next_series_seasons did not return anything
  imdb_lookup:
    - lookup failed
  tmdb_lookup:
    - TMDB lookup failed
  api_bluray:
    - Error refreshing movie details